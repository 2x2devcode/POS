Building POS

Native Linux (Ubuntu 18.04 / 20.04 / 22.04 / 24.04 / 26.04)
-----------------------------------------------------------
Automated build (detects the host Ubuntu version, installs deps,
builds CLI + GUI into release/linux/<distro>-<version>/):

  ./compile-linux.sh

Manual steps: see doc/build-unix.txt.

Windows cross-compile from Ubuntu 22.04
---------------------------------------
Builds both CLI (posd.exe) and GUI (pos-qt.exe). See doc/build-windows.txt:

  ./compile-windows.sh

Output: release/windows/posd.exe and release/windows/pos-qt.exe

See doc/readme-qt.rst for additional Qt options.
See doc/build-*.txt for platform-specific notes on building posd,
the intended-for-services, no-graphical-interface, reference
implementation of POS.
