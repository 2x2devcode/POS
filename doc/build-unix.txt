UNIX BUILD NOTES
================

Supported hosts (native Linux builds)
-------------------------------------
  Ubuntu 18.04 LTS (Bionic) — use g++-8 (installed by compile-linux.sh)
  Ubuntu 20.04 LTS (Focal)
  Ubuntu 22.04 LTS (Jammy)
  Ubuntu 24.04 LTS (Noble)
  Ubuntu 26.04 LTS (Resolute) and newer

The source tree includes OpenSSL 1.1/3.x and Boost 1.70–1.83+
compatibility layers (src/openssl_compat.h, src/boost_compat.h,
updated src/bignum.h).

Automated build (recommended)
-----------------------------

  ./compile-linux.sh

Detects the host Ubuntu/Debian version, installs matching packages,
and writes binaries to:

  release/linux/<distro>-<version>/posd
  release/linux/<distro>-<version>/pos-qt

Useful environment variables:

  SKIP_APT=1       Skip apt installs
  BUILD_GUI=0      CLI only
  BUILD_CLI=0      GUI only
  CLEAN_FIRST=1    Clean before building
  USE_UPNP=-       Disable UPnP (default)
  USE_QRCODE=1     Enable QR code support
  JOBS=N           Parallel make jobs

Note: binaries are linked for the host glibc. A build on Ubuntu 24.04
will not run on 18.04/20.04 — run the script on each target series
(or in a matching container) to produce per-version packages.

Manual build
------------

  # Headless daemon
  cd src/
  make -f makefile.unix -j$(nproc)
  strip posd

  # Qt5 GUI
  # (from repository root)
  qmake USE_UPNP=- pos-qt.pro
  make -j$(nproc)

See readme-qt.rst for additional Qt options.

Dependencies
------------

 Library     Purpose           Description
 -------     -------           -----------
 libssl      SSL Support       Secure communications (OpenSSL 1.1 or 3.x)
 libdb++     Berkeley DB       Blockchain & wallet storage
 libboost    Boost             C++ Library (1.70+)
 miniupnpc   UPnP Support      Optional firewall-jumping support
 libqrencode QRCode generation Optional QRCode generation
 Qt5         GUI               pos-qt wallet

miniupnpc may be used for UPnP port mapping. Set USE_UPNP:
 USE_UPNP=-    No UPnP support - miniupnpc not required
 USE_UPNP=0    UPnP support turned off by default at runtime
 USE_UPNP=1    UPnP support turned on by default at runtime

libqrencode: Set USE_QRCODE=1 to enable.

Dependency install: Ubuntu 22.04 / 24.04 / 26.04
------------------------------------------------

  sudo apt-get update
  sudo apt-get install -y \
    build-essential libssl-dev libdb++-dev libboost-all-dev \
    libminiupnpc-dev libqrencode-dev \
    qtbase5-dev qtbase5-dev-tools qttools5-dev-tools \
    libqt5gui5 libqt5core5a libqt5dbus5 qtchooser

Dependency install: Ubuntu 18.04 / 20.04
----------------------------------------

  sudo apt-get update
  sudo apt-get install -y \
    build-essential g++-8 libssl-dev libdb++-dev libboost-all-dev \
    libminiupnpc-dev libqrencode-dev \
    qt5-default qtbase5-dev qtbase5-dev-tools qttools5-dev-tools \
    libqt5gui5 libqt5core5a libqt5dbus5

  # On 18.04 prefer g++-8:
  #   export CC=gcc-8 CXX=g++-8

Notes:
  * Package "qt5-default" was removed after Ubuntu 20.04 — use
    qtbase5-dev + qtchooser instead.
  * On Ubuntu 24.04+ some libraries use the "t64" time_t transition
    package names; apt resolves libssl-dev / libdb++-dev correctly.
  * System OpenSSL 3.x is supported. Legacy SHA256_* APIs are allowed
    via -DOPENSSL_SUPPRESS_DEPRECATED in the makefiles.
  * Prefer ./compile-linux.sh — it selects the correct package set
    for the detected Ubuntu version automatically.

Windows cross-compile
---------------------
See doc/build-windows.txt and ./compile-windows.sh (Ubuntu 22.04 host).

Security
--------
See previous notes on PIE and non-executable stack in older docs.
To build with PIE:
  make -f makefile.unix PIE=1
